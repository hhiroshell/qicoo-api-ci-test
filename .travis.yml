language: go

go:
  - "1.11.x"

before_install:
  - go get github.com/golang/lint/golint
  - go get github.com/golang/dep/cmd/dep

install: true

services:
  - mysql

script: 
  - dep ensure
  - make create-dotenv
  - cat .env
  - make && make test